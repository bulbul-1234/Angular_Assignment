import { ApplyDirective } from './apply.directive';

describe('ApplyDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyDirective();
    expect(directive).toBeTruthy();
  });
});
